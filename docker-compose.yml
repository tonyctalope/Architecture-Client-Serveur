version: '3.8'
services:
  db:
    image: mariadb
    ports:
      - "3306:3306"
    environment:
      - MARIADB_ROOT_PASSWORD=root
      - MARIADB_DATABASE=ubeer
  adminer:
    image: adminer
    ports:
      - "8080:8080"